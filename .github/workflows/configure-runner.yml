name: Create Selfhosted Runner

on:
  workflow_dispatch:
    inputs:
      RUNNER_TOKEN:
        description: 'This is the runner token, not PAT'
        required: true

jobs:
  run-as-job:
    runs-on: ubuntu-latest
    # We set these at the job level so every step sees them automatically
    env:
      CRAVE_USERNAME: ${{ secrets.CRAVE_USERNAME }}
      CRAVE_TOKEN: ${{ secrets.CRAVE_TOKEN }}
      CRAVE_HOST: https://foss.crave.io
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install and Test Crave
        run: |
          # 1. Install tool
          curl -s https://raw.githubusercontent.com/accupara/crave/master/get_crave.sh | bash -s --
          sudo mv crave /usr/local/bin/
          
          # 2. Check if the tool recognizes your account via environment variables
          echo "Testing connection to FOSS..."
          crave list projects

      - name: Start Runner via LOS 22.1
        run: |
          # 3. Create the mapping file specifically for your active dashboard project
          cat <<EOF > crave.yaml
          project: LOS 22.1
          EOF
          
          # 4. Fire the runner
          crave run --no-patch -- " \
            rm -rf actions-runner || true && \
            mkdir actions-runner && \
            cd actions-runner && \
            curl -o actions-runner-linux-x64-2.333.0.tar.gz -L https://github.com/actions/runner/releases/download/v2.333.0/actions-runner-linux-x64-2.333.0.tar.gz && \
            tar xzf ./actions-runner-linux-x64-2.333.0.tar.gz && \
            ./config.sh --unattended --url https://github.com/${{ github.repository }} --token ${{ github.event.inputs.RUNNER_TOKEN }} --replace && \
            ./run.sh \
          "
